const fs = require('fs');
// This is frontend code, I can't query firebase from here easily because I'm on a local machine without firebase admin set up, or am I?
console.log("I can't run this easily")
